const React = require('react');
const Display = require('./components/Display');

let UI = React.createClass({
    render: function() {
        return (
            <Display/>
        );
    },
});

module.exports = UI;
